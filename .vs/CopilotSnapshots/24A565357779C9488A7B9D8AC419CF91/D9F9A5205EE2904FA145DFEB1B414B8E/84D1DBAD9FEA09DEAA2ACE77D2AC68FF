using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace ShoppingListAW4E.Models
{
    // Klasa reprezentuje pojedynczy produkt.
    // Wyjaśnienie prostym językiem:
    // - Produkt ma nazwę, jednostkę (np. "kg"), ilość oraz informację, do której
    // kategorii należy.
    // - Pole IsBought mówi, czy przedmiot został już kupiony.
    // - Implementujemy INotifyPropertyChanged aby widok aktualizował się automatycznie
    // gdy zmieni się np. ilość produktu.
    public class Product : INotifyPropertyChanged
    {
        int quantity;
        bool isBought;

        public string Name { get; set; }
        public string Unit { get; set; }
        public string CategoryName { get; set; }

        public int Quantity
        {
            get => quantity;
            set
            {
                quantity = value;
                OnPropertyChanged();
            }
        }

        public bool IsBought
        {
            get => isBought;
            set
            {
                isBought = value;
                OnPropertyChanged();
            }
        }

        public event PropertyChangedEventHandler PropertyChanged;

        void OnPropertyChanged([CallerMemberName] string name = "")
        {
            PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
        }
    }
}